class MenuItem {
  String itemName,
      itemPrice,
      itemCount,
      itemImage,
      itemCategory,
      itemDescription,
      itemType;

  MenuItem(this.itemName, this.itemPrice, this.itemCount, this.itemImage,
      this.itemCategory, this.itemDescription, this.itemType);
}
